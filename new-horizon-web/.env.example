VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
# Optional: alternative publishable key (used by BACKEND_READY check in App.jsx)
# VITE_SUPABASE_PUBLISHABLE_KEY=your-publishable-key

# ── Integrations (Google OAuth) ─────────────────────────────────────────
# Public Google OAuth Web Application client id (inlined into the SPA
# bundle by Vite). Register this app's origin +
#   <origin>/integrations/callback
# as an authorized redirect URI in Google Cloud Console.
VITE_GOOGLE_OAUTH_CLIENT_ID=

# Optional override for the Supabase Edge Function URL. Defaults to
# `${VITE_SUPABASE_URL}/functions/v1/integrations`.
# VITE_INTEGRATIONS_FUNCTION_URL=
