{
  "name": "new-horizon-platform",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "workspaces": [
    "new-horizon-web",
    "mobile"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=new-horizon-web",
    "build": "npm run build --workspace=new-horizon-web",
    "lint": "npm run lint --workspace=new-horizon-web",
    "start": "npm run start --workspace=new-horizon-web",
    "preview": "npm run preview --workspace=new-horizon-web",
    "mobile": "npm run start --workspace=mobile",
    "mobile:android": "npm run android --workspace=mobile",
    "mobile:ios": "npm run ios --workspace=mobile"
  }
}
